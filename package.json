{
  "name": "searchable-data-log",
  "description": "A Data log store which is fast to save and search.",
  "version": "1.0.0",
  "author": "FujiHaruka",
  "dependencies": {
    "bind-decorator": "^1.0.11",
    "fs-extra": "^5.0.0",
    "lodash": "^4.17.5",
    "queue": "^4.4.2",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.1",
    "@types/lodash": "^4.14.104",
    "@types/mocha": "^2.2.48",
    "@types/uuid": "^3.4.3",
    "assert-throws-async": "^2.0.1",
    "espower-typescript": "^8.1.3",
    "power-assert": "^1.4.4",
    "tslint": "^5.9.1",
    "tslint-config-standard": "^7.0.0",
    "typescript": "^2.7.2"
  },
  "license": "MIT",
  "main": "lib",
  "private": true,
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "lint": "fixpack && tslint --format stylish --fix --project tsconfig.json",
    "test": "mocha --require espower-typescript/guess",
    "watch": "npm run clean && tsc -w"
  }
}
